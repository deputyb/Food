(function (wegmans) {
    'use strict';

    wegmans.views.eyebrowNavigation = Backbone.View.extend({

        /**
         * Initialize
         */
        initialize: function () {
            console.log('eyebrow navigation => initialized');
        }
    });
}(wegmans));